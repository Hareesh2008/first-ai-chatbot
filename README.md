# first-ai-chatbot
to note before running 
please run these below commends and enter ur gemini api key before running and create a predefined data as per mentioned in youtube tutorial .
pip install streamlit
pip install google-genai
pip install pyttsx3
pip install SpeechRecognition
pip install streamlit-mic-recorder
pip install pydub
winget install ffmpeg
ffmpeg -version ##to verify version
streamlit run assistive_ai_app.py
